---
- name         : Install and configure fastd
  hosts        : all
  gather_facts : yes
  connection   : "{{ kitchen_connection | default('local') }}"
  roles        :
                 - "ansible-announced"